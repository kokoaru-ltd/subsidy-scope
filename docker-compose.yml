services:
  postgres:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: subsidy_scope
      POSTGRES_PASSWORD: local_dev_password
      POSTGRES_DB: subsidy_scope
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
